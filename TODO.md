# TODO - Fix resume modal + PDF link + LinkedIn URL

## Plan items
- [x] Update hero Resume link to correct PDF filename (`vignesh_resume.pdf`) and open modal or new tab (based on chosen behavior).
- [x] Make modal iframe PDF path robust and consistent with existing file.
- [ ] Ensure resume modal opens by wiring correct trigger element to `#resumeBtn` in `script.js`.
- [x] Fix LinkedIn URL in `home.js` if it doesn’t match the provided profile.
- [ ] Run `index.html` using a Windows-safe command and verify resume modal and PDF load (use `file://` open if start command keeps failing).



