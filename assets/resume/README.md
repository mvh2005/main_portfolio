# Resume Upload

The resume buttons now use the PDF in this folder instead:

```text
sections/resume-modal/resume_vignesh.pdf
```

Keep the PDF there unless you also update the links in `sections/home/home.js` and `sections/resume-modal/resume-modal.js`.
